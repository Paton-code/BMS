<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paton.mapper.BookMapper">
  <resultMap id="BaseResultMap" type="com.paton.domain.Book">
    <id column="book_id" jdbcType="INTEGER" property="bookId" />
    <result column="book_name" jdbcType="VARCHAR" property="bookName" />
    <result column="book_author" jdbcType="VARCHAR" property="bookAuthor" />
    <result column="book_publish" jdbcType="VARCHAR" property="bookPublish" />
    <result column="book_category" jdbcType="INTEGER" property="bookCategory" />
    <result column="book_price" jdbcType="DOUBLE" property="bookPrice" />
    <result column="book_introduction" jdbcType="VARCHAR" property="bookIntroduction" />
    <!-- 新增字段：图书封面和描述 -->
    <result column="cover_image" jdbcType="VARCHAR" property="coverImage" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="stock_count" jdbcType="INTEGER" property="stock" />
  </resultMap>

  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

  <sql id="Update_By_Example_Where_Clause">
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

  <sql id="Base_Column_List">
    book_id, book_name, book_author, book_publish, book_category, book_price, book_introduction, cover_image, description, stock_count
  </sql>

  <!-- 基础CRUD操作 -->
  <select id="selectByExample" parameterType="com.paton.domain.BookExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    'true' as QUERYID,
    <include refid="Base_Column_List" />
    from book
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>

  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from book
    where book_id = #{bookId,jdbcType=INTEGER}
  </select>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from book
    where book_id = #{bookId,jdbcType=INTEGER}
  </delete>

  <delete id="deleteByExample" parameterType="com.paton.domain.BookExample">
    delete from book
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </delete>

  <insert id="insert" parameterType="com.paton.domain.Book">
    insert into book (book_id, book_name, book_author,
                      book_publish, book_category, book_price,
                      book_introduction, cover_image, description)
    values (#{bookId,jdbcType=INTEGER}, #{bookName,jdbcType=VARCHAR}, #{bookAuthor,jdbcType=VARCHAR},
            #{bookPublish,jdbcType=VARCHAR}, #{bookCategory,jdbcType=INTEGER}, #{bookPrice,jdbcType=DOUBLE},
            #{bookIntroduction,jdbcType=VARCHAR}, #{coverImage,jdbcType=VARCHAR}, #{description,jdbcType=VARCHAR})
  </insert>

  <insert id="insertSelective" parameterType="com.paton.domain.Book">
    insert into book
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="bookId != null">
        book_id,
      </if>
      <if test="bookName != null">
        book_name,
      </if>
      <if test="bookAuthor != null">
        book_author,
      </if>
      <if test="bookPublish != null">
        book_publish,
      </if>
      <if test="bookCategory != null">
        book_category,
      </if>
      <if test="bookPrice != null">
        book_price,
      </if>
      <if test="bookIntroduction != null">
        book_introduction,
      </if>
      <if test="coverImage != null">
        cover_image,
      </if>
      <if test="description != null">
        description,
      </if>
      <if test="stock != null">
        stock_count,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="bookId != null">
        #{bookId,jdbcType=INTEGER},
      </if>
      <if test="bookName != null">
        #{bookName,jdbcType=VARCHAR},
      </if>
      <if test="bookAuthor != null">
        #{bookAuthor,jdbcType=VARCHAR},
      </if>
      <if test="bookPublish != null">
        #{bookPublish,jdbcType=VARCHAR},
      </if>
      <if test="bookCategory != null">
        #{bookCategory,jdbcType=INTEGER},
      </if>
      <if test="bookPrice != null">
        #{bookPrice,jdbcType=DOUBLE},
      </if>
      <if test="bookIntroduction != null">
        #{bookIntroduction,jdbcType=VARCHAR},
      </if>
      <if test="coverImage != null">
        #{coverImage,jdbcType=VARCHAR},
      </if>
      <if test="description != null">
        #{description,jdbcType=VARCHAR},
      </if>
      <if test="stock != null">
        #{stock,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>

  <select id="countByExample" parameterType="com.paton.domain.BookExample" resultType="java.lang.Long">
    select count(*) from book
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>

  <update id="updateByExampleSelective" parameterType="map">
    update book
    <set>
      <if test="record.bookId != null">
        book_id = #{record.bookId,jdbcType=INTEGER},
      </if>
      <if test="record.bookName != null">
        book_name = #{record.bookName,jdbcType=VARCHAR},
      </if>
      <if test="record.bookAuthor != null">
        book_author = #{record.bookAuthor,jdbcType=VARCHAR},
      </if>
      <if test="record.bookPublish != null">
        book_publish = #{record.bookPublish,jdbcType=VARCHAR},
      </if>
      <if test="record.bookCategory != null">
        book_category = #{record.bookCategory,jdbcType=INTEGER},
      </if>
      <if test="record.bookPrice != null">
        book_price = #{record.bookPrice,jdbcType=DOUBLE},
      </if>
      <if test="record.bookIntroduction != null">
        book_introduction = #{record.bookIntroduction,jdbcType=VARCHAR},
      </if>
      <if test="record.coverImage != null">
        cover_image = #{record.coverImage,jdbcType=VARCHAR},
      </if>
      <if test="record.description != null">
        description = #{record.description,jdbcType=VARCHAR},
      </if>
      <if test="record.stock != null">
        stock = #{record.stock,jdbcType=INTEGER},
      </if>
    </set>
    <if test="_parameter != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>

  <update id="updateByExample" parameterType="map">
    update book
    set book_id = #{record.bookId,jdbcType=INTEGER},
    book_name = #{record.bookName,jdbcType=VARCHAR},
    book_author = #{record.bookAuthor,jdbcType=VARCHAR},
    book_publish = #{record.bookPublish,jdbcType=VARCHAR},
    book_category = #{record.bookCategory,jdbcType=INTEGER},
    book_price = #{record.bookPrice,jdbcType=DOUBLE},
    book_introduction = #{record.bookIntroduction,jdbcType=VARCHAR},
    cover_image = #{record.coverImage,jdbcType=VARCHAR},
    description = #{record.description,jdbcType=VARCHAR},
    stock = #{record.stock,jdbcType=INTEGER}
    <if test="_parameter != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>

  <update id="updateByPrimaryKeySelective" parameterType="com.paton.domain.Book">
    update book
    <set>
      <if test="bookName != null">
        book_name = #{bookName,jdbcType=VARCHAR},
      </if>
      <if test="bookAuthor != null">
        book_author = #{bookAuthor,jdbcType=VARCHAR},
      </if>
      <if test="bookPublish != null">
        book_publish = #{bookPublish,jdbcType=VARCHAR},
      </if>
      <if test="bookCategory != null">
        book_category = #{bookCategory,jdbcType=INTEGER},
      </if>
      <if test="bookPrice != null">
        book_price = #{bookPrice,jdbcType=DOUBLE},
      </if>
      <if test="bookIntroduction != null">
        book_introduction = #{bookIntroduction,jdbcType=VARCHAR},
      </if>
      <if test="coverImage != null">
        cover_image = #{coverImage,jdbcType=VARCHAR},
      </if>
      <if test="description != null">
        description = #{description,jdbcType=VARCHAR},
      </if>
      <if test="stock != null">
        stock_count = #{stock,jdbcType=INTEGER},
      </if>
    </set>
    where book_id = #{bookId,jdbcType=INTEGER}
  </update>

  <update id="updateByPrimaryKey" parameterType="com.paton.domain.Book">
    update book
    set book_name = #{bookName,jdbcType=VARCHAR},
        book_author = #{bookAuthor,jdbcType=VARCHAR},
        book_publish = #{bookPublish,jdbcType=VARCHAR},
        book_category = #{bookCategory,jdbcType=INTEGER},
        book_price = #{bookPrice,jdbcType=DOUBLE},
        book_introduction = #{bookIntroduction,jdbcType=VARCHAR},
        cover_image = #{coverImage,jdbcType=VARCHAR},
        description = #{description,jdbcType=VARCHAR},
        stock_count = #{stock,jdbcType=INTEGER}
    where book_id = #{bookId,jdbcType=INTEGER}
  </update>

  <!-- ==================== 新增查询方法 ==================== -->

  <!-- 按分类ID查询书籍（分页） -->
  <select id="selectByCategoryIdWithPagination" parameterType="map" resultMap="BaseResultMap">
    SELECT * FROM book
    WHERE book_category = #{categoryId,jdbcType=INTEGER}
      LIMIT #{offset,jdbcType=INTEGER}, #{limit,jdbcType=INTEGER}
  </select>

  <!-- 按分类ID查询书籍（不分页） -->
  <select id="selectByCategoryId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    SELECT * FROM book
    WHERE book_category = #{categoryId,jdbcType=INTEGER}
  </select>

  <!-- 按分类ID统计书籍数量 -->
  <select id="selectBookCountByCategoryId" parameterType="java.lang.Integer" resultType="java.lang.Integer">
    SELECT COUNT(*) FROM book
    WHERE book_category = #{categoryId,jdbcType=INTEGER}
  </select>

  <!-- 按书名模糊查询 -->
  <select id="selectByBookName" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT * FROM book
    WHERE book_name LIKE CONCAT('%', #{bookName,jdbcType=VARCHAR}, '%')
  </select>

  <!-- 查询所有书籍 -->
  <select id="selectAll" resultMap="BaseResultMap">
    SELECT * FROM book
  </select>

  <!-- 分页查询图书（带限制） -->
  <select id="selectByExampleWithLimit" parameterType="map" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from book
    <if test="example != null">
      <where>
        <foreach collection="example.oredCriteria" item="criteria" separator="or">
          <if test="criteria.valid">
            <trim prefix="(" prefixOverrides="and" suffix=")">
              <foreach collection="criteria.criteria" item="criterion">
                <choose>
                  <when test="criterion.noValue">
                    and ${criterion.condition}
                  </when>
                  <when test="criterion.singleValue">
                    and ${criterion.condition} #{criterion.value}
                  </when>
                  <when test="criterion.betweenValue">
                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                  </when>
                  <when test="criterion.listValue">
                    and ${criterion.condition}
                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                      #{listItem}
                    </foreach>
                  </when>
                </choose>
              </foreach>
            </trim>
          </if>
        </foreach>
      </where>
    </if>
    <if test="example != null and example.orderByClause != null">
      order by ${example.orderByClause}
    </if>
    limit #{offset,jdbcType=INTEGER}, #{limit,jdbcType=INTEGER}
  </select>

  <!-- 查询所有分类信息 -->
  <select id="selectAllCategories" resultType="java.util.HashMap">
    SELECT category_id as categoryId, category_name as categoryName
    FROM book_category
  </select>

  <!-- 查询所有书籍的库存信息（含已借出数量和可用数量） -->
  <select id="selectAllBooksStockInfo" resultType="java.util.HashMap">
    SELECT
      b.book_id as bookId,
      b.book_name as bookName,
      b.book_author as bookAuthor,
      b.book_publish as bookPublish,
      b.book_category as bookCategory,
      IFNULL(b.stock_count, 1) as totalStock,
      IFNULL(COUNT(bb.id), 0) as borrowedCount,
      IFNULL(b.stock_count, 1) - IFNULL(COUNT(bb.id), 0) as availableCount
    FROM book b
           LEFT JOIN borrowing_books bb ON b.book_id = bb.book_id
    GROUP BY b.book_id, b.book_name, b.book_author, b.book_publish, b.book_category, b.stock_count
    ORDER BY b.book_category, b.book_id
  </select>
</mapper>